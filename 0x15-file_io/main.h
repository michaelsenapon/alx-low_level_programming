#ifndef _MAIN_H_
#define _MAIN_H_

/* Standard C library headers */
#include <fcntl.h>
#include <unistd.h>
#include <stdlib.h>

/* Function prototypes */
int _putchar(char c);
ssize_t read_textfile(const char *filename, size_t letters);

#endif /* _MAIN_H_ */
