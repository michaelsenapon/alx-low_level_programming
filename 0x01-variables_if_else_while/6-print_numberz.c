#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int digit = 48;

	for (; digit <= 57; digit++)
	{
		putchar(digit);
	}
	putchar('\n');
	return (0);
}
