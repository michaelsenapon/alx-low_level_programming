#include "main.h"

/**
 * add - Add two integers and return the result
 * @i: first parameter for addition
 * @j: second parameter for addition
 *
 * Return: Sum of the parameters
 */
int add(int i, int j)
{
	return (i + j);
}
